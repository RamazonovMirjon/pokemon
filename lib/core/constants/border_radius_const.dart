import 'package:flutter/material.dart';

class BorderRadiusConst {
  static BorderRadius kExtraSmallBorderRadius =
      const BorderRadius.all(Radius.circular(10));
  static BorderRadius kSmallBorderRadius =
      const BorderRadius.all(Radius.circular(20));
  static BorderRadius kMediumBorderRadius =
      const BorderRadius.all(Radius.circular(30));
  static BorderRadius kLargeBorderRadius =
      const BorderRadius.all(Radius.circular(40));
  static BorderRadius kExtralargeBorderRadius =
      const BorderRadius.all(Radius.circular(60));
}
